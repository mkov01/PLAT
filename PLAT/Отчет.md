# 📊 ОТЧЕТ ПО ЛАБОРАТОРНОЙ РАБОТЕ №1
> **Тема:** Основы Python. Переменные, типы данных, ввод/вывод и арифметические операции  
> **Выполнил:** Мальков Александр / ИС 25 22    
> **Среда разработки:** Visual Studio Code / Python 3.x  

---

## 1. Номер и условия вариантов

* **Задание №2 (Прямоугольник):** Введите длину и ширину прямоугольника. Найдите его площадь и периметр.
* **Задание №10 (Поразрядный разбор):** Введите четырехзначное число. Выведите его цифры по отдельности: тысячи, сотни, десятки и единицы.
* **Задание №17 (Капитализация вклада):** Введите сумму вклада, годовую процентную ставку и количество лет. Рассчитайте итоговую сумму с учетом ежегодной капитализации процентов.

---

## 2. Цель работы

Освоить структуру стандартной программы на языке Python, работу с функциями ввода-вывода (`input()`, `print()`), переменными, преобразованиями типов (`int()`, `float()`), математическими операторами (`//`, `%`, `**`), а также разработку линейных алгоритмов и построение консольного пользовательского интерфейса.

---

## 3. Входные и выходные данные

### Задание №2:
* **Входные данные:** Длина $a \in \mathbb{R}^+$, ширина $b \in \mathbb{R}^+$ (числа с плавающей точкой).
* **Выходные данные:** Площадь $S \in \mathbb{R}^+$, периметр $P \in \mathbb{R}^+$.

### Задание №10:
* **Входные данные:** Четырехзначное целое число $num \in \mathbb{Z} \cap [1000, 9999]$.
* **Выходные данные:** Четыре целых числа — разряды тысяч, сотен, десятков и единиц.

### Задание №17:
* **Входные данные:** Начальная сумма вклада $P \ge 0$, процентная ставка $r \ge 0$, срок вклада в годах $n \in \mathbb{Z}^+$.
* **Выходные данные:** Итоговая сумма на счете $A$, чистая прибыль $Profit$.

---

## 4. Алгоритм решения

1. **Задание №2:** 
   * Считать значения длины $a$ и ширины $b$.
   * Вычислить площадь по формуле: $S = a \times b$.
   * Вычислить периметр по формуле: $P = 2 \times (a + b)$.
   * Вывести результаты с округлением до двух знаков после запятой.

2. **Задание №10:**
   * Считать целое четырехзначное число $num$.
   * Используя целочисленное деление `//` и остаток от деления `%`, выделить разряды:
     * Тысячи: $num // 1000$
     * Сотни: $(num \% 1000) // 100$
     * Десятки: $(num \% 100) // 10$
     * Единицы: $num \% 10$
   * Вывести полученные цифры по отдельности.

3. **Задание №17:**
   * Считать сумму вклада $P$, процент $r$, срок $n$.
   * Рассчитать итоговую сумму по формуле сложных процентов: $A = P \times \left(1 + \frac{r}{100}\right)^n$.
   * Вычислить прибыль: $Profit = A - P$.
   * Вывести финансовый отчет.

---

## 5. Исходный код Python

import os
import sys

def clear_screen():
    """Очистка консоли для красивого переключения между задачами"""
    os.system('cls' if os.name == 'nt' else 'clear')

def print_header(title):
    """Красивая рамка для заголовков"""
    print("\n" + "=" * 55)
    print(f"║ {title.center(51)} ║")
    print("=" * 55)

def task_2():
    clear_screen()
    print_header("ЗАДАНИЕ №2: РАСЧЕТ ПРЯМОУГОЛЬНИКА")
    try:
        a = float(input("  ► Введите длину прямоугольника: "))
        b = float(input("  ► Введите ширину прямоугольника: "))
        
        if a <= 0 or b <= 0:
            print("\n  [!] Ошибка: Стороны должны быть строго больше нуля!")
            return
            
        S = a * b
        P = 2 * (a + b)
        
        print("\n" + "-" * 55)
        print(f"  ✔ Результаты вычислений:")
        print(f"    • Площадь (S) : {S:,.2f}")
        print(f"    • Периметр (P): {P:,.2f}")
        print("-" * 55)
        
    except ValueError:
        print("\n  [!] Ошибка: Пожалуйста, вводите только числа. Для дробей — точка.")

def task_10():
    clear_screen()
    print_header("ЗАДАНИЕ №10: ПОРАЗРЯДНЫЙ РАЗБОР ЧИСЛА")
    try:
        num = int(input("  ► Введите четырехзначное целое число: "))
        
        if not (1000 <= abs(num) <= 9999):
            print("\n  [!] Ошибка: Число должно содержать ровно 4 цифры!")
            return
            
        num = abs(num)
        thousands = num // 1000
        hundreds = (num % 1000) // 100
        tens = (num % 100) // 10
        units = num % 10
        
        print("\n" + "-" * 55)
        print("  ✔ Выделенные разряды:")
        print(f"    • Тысячи : {thousands}")
        print(f"    • Сотни   : {hundreds}")
        print(f"    • Десятки : {tens}")
        print(f"    • Единицы : {units}")
        print("-" * 55)
        
    except ValueError:
        print("\n  [!] Ошибка: Введено не целое число!")

def task_17():
    clear_screen()
    print_header("ЗАДАНИЕ №17: КАПИТАЛИЗАЦИЯ ВКЛАДА")
    try:
        principal = float(input("  ► Введите начальную сумму вклада: "))
        rate = float(input("  ► Введите годовую ставку (%): "))
        years = int(input("  ► Введите срок вклада (лет): "))
        
        if principal < 0 or rate < 0 or years < 0:
            print("\n  [!] Ошибка: Параметры не могут быть отрицательными.")
            return
            
        amount = principal * ((1 + rate / 100) ** years)
        profit = amount - principal
        
        print("\n" + "-" * 55)
        print("  ✔ Финансовый отчет:")
        print(f"    • Итоговая сумма на счете: {amount:,.2f}")
        print(f"    • Чистая прибыль         : {profit:,.2f}")
        print("-" * 55)
        
    except ValueError:
        print("\n  [!] Ошибка: Неверный формат данных.")

def main_menu():
    while True:
        clear_screen()
        print("=" * 55)
        print("║" + " ГЛАВНОЕ МЕНЮ ЛАБОРАТОРНОЙ РАБОТЫ ".center(53, " ") + "║")
        print("=" * 55)
        print("  [1] Задание №2  (Площадь и периметр прямоугольника)")
        print("  [2] Задание №10 (Разбор четырехзначного числа)")
        print("  [3] Задание №17 (Ежегодная капитализация вклада)")
        print("  [0] Выход из программы")
        print("=" * 55)
        
        choice = input("  ► Выберите номер пункта меню: ").strip()
        
        if choice == '1':
            task_2()
        elif choice == '2':
            task_10()
        elif choice == '3':
            task_17()
        elif choice == '0':
            clear_screen()
            print("\n  Программа успешно завершена. Всего доброго!\n")
            sys.exit()
        else:
            print("\n  [!] Неверный пункт. Нажмите Enter, чтобы попробовать снова...")
            
        input("\n  Нажмите Enter для возврата в меню...")

if __name__ == "__main__":
    main_menu()

## 6. Скриншот выполнения программы.
    
<img width="521" height="519" alt="image" src="https://github.com/user-attachments/assets/315514a5-59a0-4dca-be96-363d268204d1" />

## 7. Не менее трех тестовых примеров.

Тест №1: Задание №2 (Прямоугольник)Входные данные: $a = 7.5$, $b = 3.2$Ожидаемый результат:Площадь ($S$): $24.00$Периметр ($P$): $21.40$Статус проверки: Успешно пройдено. Программа корректно обрабатывает вещественные числа и форматирует вывод до двух знаков после запятой.

Тест №2: Задание №10 (Поразрядный разбор)Входные данные: $num = 8914$Ожидаемый результат:Тысячи: $8$Сотни: $9$Десятки: $1$Единицы: $4$Статус проверки: Успешно пройдено. Логика с использованием операторов // и % отработала корректно без погрешностей округления.

Тест №3: Задание №17 (Капитализация вклада)Входные данные: $principal = 150000.0$, $rate = 8.5$, $years = 3$Ожидаемый результат:Итоговая сумма на счете: $191,241.65$Чистая прибыль: $41,241.65$Статус проверки: Успешно пройдено. Формула сложных процентов реализована верно, результаты выведены с разделителями тысяч и финансовым округлением.

## 8. Вывод по работе.

В ходе выполнения лабораторной работы были успешно освоены базовые концепции программирования на языке Python: синтаксис объявления переменных, механизмы ввода и вывода данных (input(), print()), а также явное и неявное преобразование типов.

Практическим путем изучены особенности арифметических операторов, в частности разница между стандартным делением, целочисленным делением (//) и вычислением остатка от деления (%), которые сыграли ключевую роль при реализации алгоритма поразрядного разбора числа. Кроме того, разработка интерактивного консольного интерфейса с защитой от некорректного ввода с помощью блоков обработки исключений (try-except) позволила повысить надежность созданного программного обеспечения и закрепить навыки структурного программирования через разделение кода на логические функции.


    
