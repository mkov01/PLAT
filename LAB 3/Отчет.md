# 📊 ОТЧЕТ ПО ЛАБОРАТОРНОЙ РАБОТЕ №3
> **Тема:** Основы объектно-ориентированного программирования (ООП). Создание классов, объектов, методов, инкапсуляция
> **Выполнил:** Мальков Александр / ИС 25 22    
> **Среда разработки:** Visual Studio Code / Python 3.x  

---

## 1. Номер и условия вариантов

* **Задание №3:** Создание класса Student с атрибутами имени, возраста и специальности. Реализация методов для вывода информации, смены специальности и изменения возраста (день рождения).
* **Задание №14:** Создание класса Password с закрытым атрибутом пароля (инкапсуляция). Реализация методов проверки пароля, безопасного изменения с проверкой старого значения и отображения статуса безопасности (с маскировкой символов).
* **Задание №18:** Создание связанных классов GroupStudent и Group для управления учебными группами: добавление студентов, удаление по имени, расчет среднего возраста и вывод списка группы.

---

## 2. Цель работы

Освоить создание классов и объектов в Python, конструктор __init__(), атрибуты, методы, self и базовую инкапсуляцию.

---

## 3. Входные и выходные данные

### Задание №3:
**Входные данные:** Строковые значения (name, specialty) и целое число (age).
**Выходные данные:** Текстовые сообщения в консоли с информацией о студенте, смене его специальности и обновлении возраста.

### Задание №14:
* **Входные данные:** Данные студентов (name, age) и название группы (group_name).
* **Выходные данные:** Статусы безопасности учетной записи (длина пароля, замаскированное представление *****) и сообщения об успешности/ошибке смены пароля.**
* 
### Задание №18:
* **Входные данные:** Три стороны треугольника $a, b, c \in \mathbb{R}^+$.
* **Выходные данные:** Список студентов выбранной группы, расчет среднего возраста с плавающей точкой и уведомления об операциях добавления/удаления.

---

## 4. Алгоритм решения

1. **Задание №3:**

Объявить класс Student с конструктором __init__, принимающим имя, возраст и специальность.

Реализовать метод display_info() для форматированного вывода данных.

Реализовать метод change_specialty(new_specialty) для обновления направления обучения.

Реализовать метод celebrate_birthday() для инкремента возраста (age += 1).

2. **Задание №14:**

Объявить класс Password, используя закрытый атрибут self._password для инкапсуляции.

Написать вспомогательный метод check_password(password_to_test) для сверки введенного пароля с текущим.

Написать метод change_password(old_password, new_password), проверяющий старый пароль и длину нового (минимум 6 символов).

Реализовать метод show_security_status(), выводящий скрытую версию пароля через звездочки.

3. **Задание №18:**

Создать вспомогательный класс GroupStudent для хранения свойств отдельного студента.

Создать основной класс Group со скрытым списком self.__students.

Реализовать методы добавления (add_student) и удаления (remove_student) по имени.

Реализовать метод вычисления среднего возраста (get_average_age) с округлением до одного знака после запятой.

---

## 5. Исходный код Python

import os
import sys

def clear_screen():
    os.system('cls' if os.name == 'nt' else 'clear')

def print_header(title):
    print("\n" + "=" * 55)
    print(f"║ {title.center(51)} ║")
    print("=" * 55)

# ЗАДАНИЕ №3: КЛАСС STUDENT
class Student:
    def __init__(self, name: str, age: int, specialty: str):
        self.name = name
        self.age = age
        self.specialty = specialty

    def display_info(self):
        print(f" Студент: {self.name} | Возраст: {self.age} | Специальность: {self.specialty}")

    def change_specialty(self, new_specialty: str):
        print(f" --> {self.name} переведен на специальность: {new_specialty}")
        self.specialty = new_specialty

    def celebrate_birthday(self):
        self.age += 1
        print(f" 🎉 У {self.name} день рождения! Теперь ему/ей {self.age} лет.")

def task_3():
    clear_screen()
    print_header("ЗАДАНИЕ №3: УПРАВЛЕНИЕ СТУДЕНТАМИ")
    s1 = Student("Адиль", 19, "ВТПО")
    s3 = Student("Руслан", 18, "Data Science")

    s1.display_info()
    s3.display_info()
    print("-" * 55)
    s1.change_specialty("Архитектура")
    s3.celebrate_birthday()
    print("-" * 55)
    s1.display_info()
    s3.display_info()

# ЗАДАНИЕ №14: КЛАСС PASSWORD
class Password:
    def __init__(self, user: str, initial_password: str):
        self.user = user
        self._password = initial_password  # Закрытый атрибут

    def check_password(self, password_to_test: str) -> bool:
        return self._password == password_to_test

    def change_password(self, old_password: str, new_password: str):
        if self.check_password(old_password):
            if len(new_password) >= 6:
                self._password = new_password
                print(f" ✅ Пароль для пользователя {self.user} успешно изменен.")
            else:
                print(" ❌ Ошибка: Новый пароль слишком короткий!")
        else:
            print(f" ❌ Ошибка: Неверный старый пароль для {self.user}!")

    def show_security_status(self):
        masked_pwd = "*" * len(self._password)
        print(f" 🔒 Пользователь: {self.user} | Длина: {len(self._password)} симв. [{masked_pwd}]")

def task_14():
    clear_screen()
    print_header("ЗАДАНИЕ №14: МЕНЕДЖЕР ПАРОЛЕЙ")
    p2 = Password("user_olga", "qwerty")
    p3 = Password("dev_team", "pass2026")

    p2.show_security_status()
    p3.show_security_status()
    print("-" * 55)
    p2.change_password("wrong_old", "new_pass")
    p3.change_password("pass2026", "secret_key")
    print("-" * 55)
    p3.show_security_status()

# ЗАДАНИЕ №18: КЛАССЫ GROUPSTUDENT И GROUP
class GroupStudent:
    def __init__(self, name: str, age: int):
        self.name = name
        self.age = age

class Group:
    def __init__(self, group_name: str):
        self.group_name = group_name
        self.__students = []

    def add_student(self, student: GroupStudent):
        self.__students.append(student)
        print(f" ➕ Студент {student.name} добавлен в группу {self.group_name}")

    def remove_student(self, student_name: str):
        for student in self.__students:
            if student.name == student_name:
                self.__students.remove(student)
                print(f" ➖ Студент {student_name} удален из группы {self.group_name}")
                return
        print(f" ❓ Студент {student_name} не найден")

    def get_average_age(self) -> float:
        if not self.__students:
            return 0.0
        return round(sum(s.age for s in self.__students) / len(self.__students), 1)

    def display_group(self):
        print(f"\n 👥 Состав группы {self.group_name} (Средний возраст: {self.get_average_age()}):")
        for student in self.__students:
            print(f"    - {student.name}, {student.age} лет")

def task_18():
    clear_screen()
    print_header("ЗАДАНИЕ №18: УПРАВЛЕНИЕ ГРУППАМИ")
    group_it = Group("ИС-25")
    group_it.add_student(GroupStudent("Медет", 19))
    group_it.add_student(GroupStudent("Айша", 21))
    
    group_it.display_group()
    print("-" * 55)
    group_it.remove_student("Айша")
    group_it.display_group()

def main_menu():
    while True:
        clear_screen()
        print("=" * 55)
        print("║" + " ГЛАВНОЕ МЕНЮ ООП ".center(53, " ") + "║")
        print("=" * 55)
        print("  [1] Задание №3  (Класс Student)")
        print("  [2] Задание №14 (Класс Password / Инкапсуляция)")
        print("  [3] Задание №18 (Классы Group и GroupStudent)")
        print("  [0] Выход из программы")
        print("=" * 55)
        
        choice = input("  ► Выберите номер пункта меню: ").strip()
        if choice == '1':
            task_3()
        elif choice == '2':
            task_14()
        elif choice == '3':
            task_18()
        elif choice == '0':
            clear_screen()
            print("\n  Программа успешно завершена. Всего доброго!\n")
            sys.exit()
        else:
            print("\n  [!] Неверный пункт меню.")
        input("\n  Нажмите Enter для возврата в меню...")

if __name__ == "__main__":
    main_menu()

## 6. Скриншот выполнения программы.

<img width="418" height="187" alt="image" src="https://github.com/user-attachments/assets/7bade4b5-3883-437a-a998-7b19b6c1bafb" />



## 7. Вывод по работе.

**Вывод:**

В ходе выполнения лабораторной работы были освоены базовые принципы объектно-ориентированного программирования (ООП) на языке Python. На практике изучены механизмы создания классов, инициализации объектов с помощью конструктора __init__ и управления их состоянием через методы класса.
Также были успешно применены концепции инкапсуляции (использование защищенных _ и приватных __ атрибутов) для ограничения прямого доступа к данным, что позволило повысить безопасность и архитектурную гибкость программного кода на примере базового, среднего и повышенного уровней сложности.


